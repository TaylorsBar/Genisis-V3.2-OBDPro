# Rebuild verification

TypeScript lint passes. The Vitest suite reports 42 passing tests with 12 skipped; one integration test expects a local service on port 8081. The PWA cache threshold is set to 12 MB for the rich OBD Pro bundle.
